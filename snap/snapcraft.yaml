name: kaku
version: 1.9.0
summary: Kaku is a highly integrated music player supports different online platforms. 

description: |
  Kaku is a highly integrated music player supports
  different online platform like YouTube, SoundCloud,
  Vimeo and more.

grade: stable
confinement: classic

parts:
  kaku:
    plugin: dump
    source: https://github.com/pdefreitas/Kaku.git
    source-type: git
    # Correct path to icon.
    prepare: |
      sed -i 's|Icon=kaku|Icon=/usr/share/kaku/logo\.png|g' usr/share/applications/kaku.desktop
    after:
      - desktop-gtk2
    stage-packages:
      - gvfs-bin
      - libasound2
      - libgconf-2-4
      - libnotify4
      - libnspr4
      - libnss3
      - libpcre3
      - libpulse0
      - libxss1
      - libxtst6

apps:
  kaku:
    command: kaku
    plugs:
      - browser-support
      - gsettings
      - home
      - mount-observe
      - network
      - pulseaudio
      - unity7
      - x11

parts:
  kaku:
    plugin: nodejs
    source: .
